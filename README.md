# プロジェクトをローカルで実行する方法

1. ターミナルを起動
2. meeq-code-testディレクトリへ移動
3. ./gradlew run --args="<プロセス数> <スレッド数> <ポート番号>"
    1. プロセス数3, スレッド数5, ポート番号8080で実行する場合:
       ```
       ./gradlew run --args="3 5 8080"
       ```